enum OrderState { available, inProgress, unavailable }

enum OrderType { donation, benefit }

enum UserType { patient, charityOrg, benefactor }

enum AuthState { waiting, notSet }

enum DataState { loading, notSet, error }
